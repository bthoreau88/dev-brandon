# Prompt - Multi-Stage Prop Assembly Animation

The scene contains a merged prop mesh with separate loose-geometry islands. Split the islands into individual named objects. Animate assembly over [DURATION] seconds: parent/base object enters first, then parts fall or slide into place with staggered timing, a small bounce/settle, and logical left-to-right or depth-based order. Capture a preview screenshot and export a preview if available.
